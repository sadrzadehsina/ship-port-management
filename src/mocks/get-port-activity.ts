import type { PortActivity } from "@/types";

export function makeData(_layTimeId: string = 'default'): PortActivity[] {
  // Return empty activities initially - users will add their own
  return [];
}
